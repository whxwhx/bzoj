#include <iostream>
#include <cstdio>
#include <algorithm>
#define rep(i,a,b) for(int i = a; i <= b; i++)
using namespace std;
const int N = 160, M = 510;
int a[N][M], f[M][N][N];
int main(){
	int n, m; scanf("%d%d",&n,&m);
	dep(i,1,n) rep(j,1,m) scanf("%d",&a[i][j]);
	rep(t1,1,n){
		
	}
}